const num1 = parseInt(prompt("ingrese un numero"));
const num2 = parseInt(prompt("ingrese otro numero"));

if (num1 > num2) {
  document.writeln(num1, "es el mayor de ambos");
} else {
  document.writeln(num2, "es el mayo de ambos");
}
